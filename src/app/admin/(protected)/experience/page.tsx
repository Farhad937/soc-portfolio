import NotYetBacked from "@/components/admin/not-yet-backed";

export default function AdminExperiencePage() {
  return (
    <NotYetBacked
      title="Experience"
      reason="No experience table exists — this content type wasn't part of the original Phase 1 schema. There's also no public page currently displaying work experience."
      phase="Phase 10"
    />
  );
}
